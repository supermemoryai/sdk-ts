# DocumentsTaskType

Task type: "memory" (default) for full context layer with SuperRAG built in, "superrag" for managed RAG as a service.

## Example Usage

```typescript
import { DocumentsTaskType } from "supermemory/models/operations";

let value: DocumentsTaskType = "memory";
```

## Values

```typescript
"memory" | "superrag"
```