# PostV3DocumentsFileTaskType

Task type: "memory" (default) for full context layer with SuperRAG built in, "superrag" for managed RAG as a service.

## Example Usage

```typescript
import { PostV3DocumentsFileTaskType } from "supermemory/models/operations";

let value: PostV3DocumentsFileTaskType = "memory";
```

## Values

```typescript
"memory" | "superrag"
```