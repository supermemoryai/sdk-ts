# GetV3DocumentsByIdTaskType

Task type: "memory" (default) for full context layer with SuperRAG built in, "superrag" for managed RAG as a service.

## Example Usage

```typescript
import { GetV3DocumentsByIdTaskType } from "supermemory/models/operations";

let value: GetV3DocumentsByIdTaskType = "memory";

// Open enum: unrecognized values are captured as Unrecognized<string>
```

## Values

```typescript
"memory" | "superrag" | Unrecognized<string>
```