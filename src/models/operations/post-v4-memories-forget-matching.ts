/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 6294f83724a0
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

export type PostV4MemoriesForgetMatchingRequest = {
  /**
   * Natural-language instruction ('forget everything about Project Titan') or a bare topic ('Project Titan'). The service searches the container's memories and selects matches to forget. Provide either query or ids.
   */
  query?: string | undefined;
  /**
   * Forget exactly these memory ids, with no semantic search. Ids are validated against the containerTag, so unknown or out-of-scope ids are ignored. Provide either query or ids.
   */
  ids?: Array<string> | undefined;
  /**
   * Container tag / space the forget operation is scoped to.
   */
  containerTag: string;
  /**
   * When true, returns the memories that WOULD be forgotten without mutating anything. Defaults to false (forgets for real).
   */
  dryRun?: boolean | undefined;
  /**
   * Minimum cosine similarity a memory must have to be considered. Lower = wider net. Defaults to 0.5.
   */
  threshold?: number | undefined;
  /**
   * Maximum number of memories this call may forget. Defaults to 100, max 500.
   */
  maxForget?: number | undefined;
  /**
   * Optional reason stored as forgetReason on each memory.
   */
  reason?: string | undefined;
};

export type Candidate = {
  /**
   * Memory ID
   */
  id: string;
  /**
   * Memory content
   */
  memory: string;
  /**
   * Similarity score against the forget query
   */
  score: number;
};

export type Forgotten = {
  /**
   * Memory ID
   */
  id: string;
  /**
   * Memory content
   */
  memory: string;
  /**
   * Similarity score against the forget query
   */
  score: number;
};

/**
 * Response after an agentic mass-forget
 */
export type PostV4MemoriesForgetMatchingResponse = {
  /**
   * Whether this was a preview (no mutation) or a real forget
   */
  dryRun: boolean;
  /**
   * Number of memories selected / forgotten
   */
  count: number;
  /**
   * ID tagged on every memory forgotten in this call (null on dryRun)
   */
  forgetBatchId: string | null;
  /**
   * The agent's one-line summary of what it did
   */
  summary: string;
  /**
   * On dryRun: the memories that would be forgotten
   */
  candidates?: Array<Candidate> | undefined;
  /**
   * On apply: the memories that were forgotten
   */
  forgotten?: Array<Forgotten> | undefined;
};

/** @internal */
export type PostV4MemoriesForgetMatchingRequest$Outbound = {
  query?: string | undefined;
  ids?: Array<string> | undefined;
  containerTag: string;
  dryRun: boolean;
  threshold: number;
  maxForget: number;
  reason?: string | undefined;
};

/** @internal */
export const PostV4MemoriesForgetMatchingRequest$outboundSchema: z.ZodMiniType<
  PostV4MemoriesForgetMatchingRequest$Outbound,
  PostV4MemoriesForgetMatchingRequest
> = z.object({
  query: z.optional(z.string()),
  ids: z.optional(z.array(z.string())),
  containerTag: z.string(),
  dryRun: z._default(z.boolean(), false),
  threshold: z._default(z.number(), 0.5),
  maxForget: z._default(z.int(), 100),
  reason: z.optional(z.string()),
});

export function postV4MemoriesForgetMatchingRequestToJSON(
  postV4MemoriesForgetMatchingRequest: PostV4MemoriesForgetMatchingRequest,
): string {
  return JSON.stringify(
    PostV4MemoriesForgetMatchingRequest$outboundSchema.parse(
      postV4MemoriesForgetMatchingRequest,
    ),
  );
}

/** @internal */
export const Candidate$inboundSchema: z.ZodMiniType<Candidate, unknown> = z
  .object({
    id: types.string(),
    memory: types.string(),
    score: types.number(),
  });

export function candidateFromJSON(
  jsonString: string,
): SafeParseResult<Candidate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Candidate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Candidate' from JSON`,
  );
}

/** @internal */
export const Forgotten$inboundSchema: z.ZodMiniType<Forgotten, unknown> = z
  .object({
    id: types.string(),
    memory: types.string(),
    score: types.number(),
  });

export function forgottenFromJSON(
  jsonString: string,
): SafeParseResult<Forgotten, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Forgotten$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Forgotten' from JSON`,
  );
}

/** @internal */
export const PostV4MemoriesForgetMatchingResponse$inboundSchema: z.ZodMiniType<
  PostV4MemoriesForgetMatchingResponse,
  unknown
> = z.object({
  dryRun: types.boolean(),
  count: types.number(),
  forgetBatchId: types.nullable(types.string()),
  summary: types.string(),
  candidates: types.optional(z.array(z.lazy(() => Candidate$inboundSchema))),
  forgotten: types.optional(z.array(z.lazy(() => Forgotten$inboundSchema))),
});

export function postV4MemoriesForgetMatchingResponseFromJSON(
  jsonString: string,
): SafeParseResult<PostV4MemoriesForgetMatchingResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      PostV4MemoriesForgetMatchingResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PostV4MemoriesForgetMatchingResponse' from JSON`,
  );
}
