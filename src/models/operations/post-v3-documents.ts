/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { smartUnion } from "../../types/smart-union.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

export type PostV3DocumentsMetadata = string | number | boolean | Array<string>;

/**
 * Task type: "memory" (default) for full context layer with SuperRAG built in, "superrag" for managed RAG as a service.
 */
export const PostV3DocumentsTaskType = {
  Memory: "memory",
  Superrag: "superrag",
} as const;
/**
 * Task type: "memory" (default) for full context layer with SuperRAG built in, "superrag" for managed RAG as a service.
 */
export type PostV3DocumentsTaskType = ClosedEnum<
  typeof PostV3DocumentsTaskType
>;

export type PostV3DocumentsFilterByMetadata =
  | string
  | number
  | boolean
  | Array<string>;

/**
 * Processing mode. "dynamic" (default) groups related documents together so memories form from coherent, logical units rather than one isolated entry at a time. "instant" processes each document on its own right away, and bills one extra operation per document.
 */
export const PostV3DocumentsDreaming = {
  Instant: "instant",
  Dynamic: "dynamic",
} as const;
/**
 * Processing mode. "dynamic" (default) groups related documents together so memories form from coherent, logical units rather than one isolated entry at a time. "instant" processes each document on its own right away, and bills one extra operation per document.
 */
export type PostV3DocumentsDreaming = ClosedEnum<
  typeof PostV3DocumentsDreaming
>;

export type PostV3DocumentsRequest = {
  /**
   * The content to extract and process into a document. This can be a URL to a website, a PDF, an image, or a video.
   */
  content: string;
  /**
   * Optional tag this document should be containerized by. Max 100 characters, alphanumeric with hyphens, underscores, and dots only.
   */
  containerTag?: string | undefined;
  /**
   * @deprecated field: This will be removed in a future release, please migrate away from it as soon as possible.
   */
  containerTags?: Array<string> | undefined;
  /**
   * Optional entity context for this container tag. Max 1500 characters. Used during document processing to guide memory extraction.
   */
  entityContext?: string | undefined;
  /**
   * Optional custom ID of the document. Max 100 characters, alphanumeric with hyphens, underscores, and dots only.
   */
  customId?: string | undefined;
  /**
   * Optional metadata for the document.
   */
  metadata?:
    | { [k: string]: string | number | boolean | Array<string> }
    | undefined;
  /**
   * Task type: "memory" (default) for full context layer with SuperRAG built in, "superrag" for managed RAG as a service.
   */
  taskType?: PostV3DocumentsTaskType | undefined;
  /**
   * Optional file path for the document. Used by supermemoryfs to store the full path of the file.
   */
  filepath?: string | undefined;
  /**
   * Optional metadata filter to apply when pulling related memories and profile during ingestion. Only memories matching these filters will be used as context.
   */
  filterByMetadata?:
    | { [k: string]: string | number | boolean | Array<string> }
    | undefined;
  /**
   * Processing mode. "dynamic" (default) groups related documents together so memories form from coherent, logical units rather than one isolated entry at a time. "instant" processes each document on its own right away, and bills one extra operation per document.
   */
  dreaming?: PostV3DocumentsDreaming | undefined;
};

/**
 * Document added successfully
 */
export type PostV3DocumentsResponse = {
  /**
   * Unique identifier of the document
   */
  id: string;
  /**
   * Status of the document
   */
  status: string;
};

/** @internal */
export type PostV3DocumentsMetadata$Outbound =
  | string
  | number
  | boolean
  | Array<string>;

/** @internal */
export const PostV3DocumentsMetadata$outboundSchema: z.ZodMiniType<
  PostV3DocumentsMetadata$Outbound,
  PostV3DocumentsMetadata
> = smartUnion([z.string(), z.number(), z.boolean(), z.array(z.string())]);

export function postV3DocumentsMetadataToJSON(
  postV3DocumentsMetadata: PostV3DocumentsMetadata,
): string {
  return JSON.stringify(
    PostV3DocumentsMetadata$outboundSchema.parse(postV3DocumentsMetadata),
  );
}

/** @internal */
export const PostV3DocumentsTaskType$outboundSchema: z.ZodMiniEnum<
  typeof PostV3DocumentsTaskType
> = z.enum(PostV3DocumentsTaskType);

/** @internal */
export type PostV3DocumentsFilterByMetadata$Outbound =
  | string
  | number
  | boolean
  | Array<string>;

/** @internal */
export const PostV3DocumentsFilterByMetadata$outboundSchema: z.ZodMiniType<
  PostV3DocumentsFilterByMetadata$Outbound,
  PostV3DocumentsFilterByMetadata
> = smartUnion([z.string(), z.number(), z.boolean(), z.array(z.string())]);

export function postV3DocumentsFilterByMetadataToJSON(
  postV3DocumentsFilterByMetadata: PostV3DocumentsFilterByMetadata,
): string {
  return JSON.stringify(
    PostV3DocumentsFilterByMetadata$outboundSchema.parse(
      postV3DocumentsFilterByMetadata,
    ),
  );
}

/** @internal */
export const PostV3DocumentsDreaming$outboundSchema: z.ZodMiniEnum<
  typeof PostV3DocumentsDreaming
> = z.enum(PostV3DocumentsDreaming);

/** @internal */
export type PostV3DocumentsRequest$Outbound = {
  content: string;
  containerTag?: string | undefined;
  containerTags?: Array<string> | undefined;
  entityContext?: string | undefined;
  customId?: string | undefined;
  metadata?:
    | { [k: string]: string | number | boolean | Array<string> }
    | undefined;
  taskType?: string | undefined;
  filepath?: string | undefined;
  filterByMetadata?:
    | { [k: string]: string | number | boolean | Array<string> }
    | undefined;
  dreaming?: string | undefined;
};

/** @internal */
export const PostV3DocumentsRequest$outboundSchema: z.ZodMiniType<
  PostV3DocumentsRequest$Outbound,
  PostV3DocumentsRequest
> = z.object({
  content: z.string(),
  containerTag: z.optional(z.string()),
  containerTags: z.optional(z.array(z.string())),
  entityContext: z.optional(z.string()),
  customId: z.optional(z.string()),
  metadata: z.optional(
    z.record(
      z.string(),
      smartUnion([z.string(), z.number(), z.boolean(), z.array(z.string())]),
    ),
  ),
  taskType: z.optional(PostV3DocumentsTaskType$outboundSchema),
  filepath: z.optional(z.string()),
  filterByMetadata: z.optional(
    z.record(
      z.string(),
      smartUnion([z.string(), z.number(), z.boolean(), z.array(z.string())]),
    ),
  ),
  dreaming: z.optional(PostV3DocumentsDreaming$outboundSchema),
});

export function postV3DocumentsRequestToJSON(
  postV3DocumentsRequest: PostV3DocumentsRequest,
): string {
  return JSON.stringify(
    PostV3DocumentsRequest$outboundSchema.parse(postV3DocumentsRequest),
  );
}

/** @internal */
export const PostV3DocumentsResponse$inboundSchema: z.ZodMiniType<
  PostV3DocumentsResponse,
  unknown
> = z.object({
  id: types.string(),
  status: types.string(),
});

export function postV3DocumentsResponseFromJSON(
  jsonString: string,
): SafeParseResult<PostV3DocumentsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PostV3DocumentsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PostV3DocumentsResponse' from JSON`,
  );
}
