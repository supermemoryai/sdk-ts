/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: d9f25d30018d
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { blobLikeSchema } from "../../types/blobs.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

export type FileT = {
  fileName: string;
  content: ReadableStream<Uint8Array> | Blob | ArrayBuffer | Uint8Array;
};

/**
 * Task type: "memory" (default) for full context layer with SuperRAG built in, "superrag" for managed RAG as a service.
 */
export const PostV3DocumentsFileTaskType = {
  Memory: "memory",
  Superrag: "superrag",
} as const;
/**
 * Task type: "memory" (default) for full context layer with SuperRAG built in, "superrag" for managed RAG as a service.
 */
export type PostV3DocumentsFileTaskType = ClosedEnum<
  typeof PostV3DocumentsFileTaskType
>;

/**
 * Processing mode. "dynamic" (default) groups related documents together so memories form from coherent, logical units rather than one isolated entry at a time. "instant" processes each document on its own right away, and bills one extra operation per document.
 */
export const PostV3DocumentsFileDreaming = {
  Instant: "instant",
  Dynamic: "dynamic",
} as const;
/**
 * Processing mode. "dynamic" (default) groups related documents together so memories form from coherent, logical units rather than one isolated entry at a time. "instant" processes each document on its own right away, and bills one extra operation per document.
 */
export type PostV3DocumentsFileDreaming = ClosedEnum<
  typeof PostV3DocumentsFileDreaming
>;

/**
 * File upload form data schema
 */
export type PostV3DocumentsFileRequest = {
  /**
   * File to upload and process
   */
  file: FileT | Blob;
  /**
   * Optional container tag (e.g., 'user_123'). Use this for a single tag.
   */
  containerTag?: string | undefined;
  /**
   * Optional container tags. Can be either a JSON string of an array (e.g., '["user_123", "project_123"]') or a single string (e.g., 'user_123'). Single strings will be automatically converted to an array.
   *
   * @deprecated field: This will be removed in a future release, please migrate away from it as soon as possible.
   */
  containerTags?: string | undefined;
  /**
   * Optional file type override to force specific processing behavior. Valid values: text, pdf, tweet, google_doc, google_slide, google_sheet, image, video, notion_doc, webpage, onedrive
   */
  fileType?: string | undefined;
  /**
   * Required when fileType is 'image' or 'video'. Specifies the exact MIME type to use (e.g., 'image/png', 'image/jpeg', 'video/mp4', 'video/webm')
   */
  mimeType?: string | undefined;
  /**
   * Optional metadata for the document as a JSON string. This is used to store additional information about the document. Keys must be strings and values can be strings, numbers, or booleans.
   */
  metadata?: string | undefined;
  /**
   * Optional metadata filter as a JSON string. Scopes which existing memories are pulled as context during ingestion. Scalar values match exactly (AND across keys); array values match ANY (OR within key).
   */
  filterByMetadata?: string | undefined;
  /**
   * Optional file path for the uploaded file (e.g., '/documents/reports/file.pdf'). Used by supermemoryfs to map documents to filesystem paths.
   */
  filepath?: string | undefined;
  /**
   * DEPRECATED: This field is no longer used. Advanced PDF processing is now automatic with our hybrid Mistral OCR + Gemini pipeline. This parameter will be accepted but ignored for backwards compatibility.
   *
   * @deprecated field: This will be removed in a future release, please migrate away from it as soon as possible.
   */
  useAdvancedProcessing?: string | undefined;
  /**
   * Optional custom ID of the document. Max 100 characters, alphanumeric with hyphens, underscores, and colons only.
   */
  customId?: string | undefined;
  /**
   * Optional entity context for this container tag. Max 1500 characters. Used during document processing to guide memory extraction.
   */
  entityContext?: string | undefined;
  /**
   * Task type: "memory" (default) for full context layer with SuperRAG built in, "superrag" for managed RAG as a service.
   */
  taskType?: PostV3DocumentsFileTaskType | undefined;
  /**
   * Processing mode. "dynamic" (default) groups related documents together so memories form from coherent, logical units rather than one isolated entry at a time. "instant" processes each document on its own right away, and bills one extra operation per document.
   */
  dreaming?: PostV3DocumentsFileDreaming | undefined;
};

/**
 * Successfully uploaded file
 */
export type PostV3DocumentsFileResponse = {
  /**
   * Unique identifier of the document
   */
  id: string;
  /**
   * Status of the document
   */
  status: string;
};

/** @internal */
export type FileT$Outbound = {
  fileName: string;
  content: ReadableStream<Uint8Array> | Blob | ArrayBuffer | Uint8Array;
};

/** @internal */
export const FileT$outboundSchema: z.ZodMiniType<FileT$Outbound, FileT> = z
  .object({
    fileName: z.string(),
    content: z.union([
      z.custom<ReadableStream<Uint8Array>>(x => x instanceof ReadableStream),
      z.custom<Blob>(x => x instanceof Blob),
      z.custom<ArrayBuffer>(x => x instanceof ArrayBuffer),
      z.custom<Uint8Array>(x => x instanceof Uint8Array),
    ]),
  });

export function fileToJSON(fileT: FileT): string {
  return JSON.stringify(FileT$outboundSchema.parse(fileT));
}

/** @internal */
export const PostV3DocumentsFileTaskType$outboundSchema: z.ZodMiniEnum<
  typeof PostV3DocumentsFileTaskType
> = z.enum(PostV3DocumentsFileTaskType);

/** @internal */
export const PostV3DocumentsFileDreaming$outboundSchema: z.ZodMiniEnum<
  typeof PostV3DocumentsFileDreaming
> = z.enum(PostV3DocumentsFileDreaming);

/** @internal */
export type PostV3DocumentsFileRequest$Outbound = {
  file: FileT$Outbound | Blob;
  containerTag?: string | undefined;
  containerTags?: string | undefined;
  fileType?: string | undefined;
  mimeType?: string | undefined;
  metadata?: string | undefined;
  filterByMetadata?: string | undefined;
  filepath?: string | undefined;
  useAdvancedProcessing?: string | undefined;
  customId?: string | undefined;
  entityContext?: string | undefined;
  taskType?: string | undefined;
  dreaming?: string | undefined;
};

/** @internal */
export const PostV3DocumentsFileRequest$outboundSchema: z.ZodMiniType<
  PostV3DocumentsFileRequest$Outbound,
  PostV3DocumentsFileRequest
> = z.object({
  file: z.union([z.lazy(() => FileT$outboundSchema), blobLikeSchema]),
  containerTag: z.optional(z.string()),
  containerTags: z.optional(z.string()),
  fileType: z.optional(z.string()),
  mimeType: z.optional(z.string()),
  metadata: z.optional(z.string()),
  filterByMetadata: z.optional(z.string()),
  filepath: z.optional(z.string()),
  useAdvancedProcessing: z.optional(z.string()),
  customId: z.optional(z.string()),
  entityContext: z.optional(z.string()),
  taskType: z.optional(PostV3DocumentsFileTaskType$outboundSchema),
  dreaming: z.optional(PostV3DocumentsFileDreaming$outboundSchema),
});

export function postV3DocumentsFileRequestToJSON(
  postV3DocumentsFileRequest: PostV3DocumentsFileRequest,
): string {
  return JSON.stringify(
    PostV3DocumentsFileRequest$outboundSchema.parse(postV3DocumentsFileRequest),
  );
}

/** @internal */
export const PostV3DocumentsFileResponse$inboundSchema: z.ZodMiniType<
  PostV3DocumentsFileResponse,
  unknown
> = z.object({
  id: types.string(),
  status: types.string(),
});

export function postV3DocumentsFileResponseFromJSON(
  jsonString: string,
): SafeParseResult<PostV3DocumentsFileResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PostV3DocumentsFileResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PostV3DocumentsFileResponse' from JSON`,
  );
}
